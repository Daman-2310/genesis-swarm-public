import { redirect } from 'next/navigation'

// RETIRED 2026-06-14 — contained fabricated press stats (€14.78B AUM, 340ms, "11 autonomous bots") + a public exit target Rebuild honestly with real figures when needed.
export default function Page() {
  redirect('/scan')
}
