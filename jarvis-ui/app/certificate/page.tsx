import { redirect } from 'next/navigation'

// RETIRED 2026-06-20 — issued pseudo-official "compliance certificates" with
// SHA3-512 / "post-quantum" / merkle-root crypto theater and a certification-
// authority implication a pre-entity tool cannot honestly make. If revived, build
// it as an honest self-assessment record — no fake crypto, no "certified" framing.
export default function CertificateRetired() {
  redirect('/scan')
}
