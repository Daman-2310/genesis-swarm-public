import { redirect } from 'next/navigation'

// RETIRED 2026-06-13 — a pre-crime-based "GENESIS-1 open standard" — unbuilt vaporware around the retired prophecy concept
export default function Page() {
  redirect('/scan')
}
