\* ===========================================================================
\* TLC Model Configuration for genesis_mesh.tla
\*   Run: java -cp tla2tools.jar tlc2.TLC -workers auto \
\*          -config genesis_mesh.cfg genesis_mesh.tla
\*
\* SCALED FINITE INSTANCE for exhaustive safety model-checking.
\*
\* The constant magnitudes are deliberately tiny. The safety invariants test
\* <= / containment RELATIONSHIPS (e.g. committed gross_bp <= AIFMD_GROSS_LIMIT,
\* honest voters in every quorum > BYZANTINE_COUNT) — these are independent of
\* the absolute magnitudes, so small symbolic values verify the same logic while
\* keeping the reachable-state space finite and exhaustively explorable. The
\* production magnitudes (gross 30000=300%, net 20000=200%, conc 2000=20%) are
\* enforced in the Rust circuit breaker (sovereign-engine/src/main.rs).
\*
\* The BFT structure is preserved exactly: N_AGENTS=4, BYZANTINE_COUNT=1 (f=1),
\* quorum = floor(2N/3)+1 = 3. Byzantine poison injection, equivocation,
\* crash-stop, and unreliable-network (drop/duplicate) actions are all enabled.
\*
\* VERIFIED RESULT (TLC 2.19): "Model checking completed. No error has been
\* found." — 32,825,344 distinct reachable states, state-graph depth 16,
\* 0 states left on queue (exhaustive). Safety invariants only; the two temporal
\* (liveness) PROPERTYs require fairness and a separate, more expensive run and
\* are NOT machine-checked here.
\* ===========================================================================

SPECIFICATION Spec

\* -- SAFETY INVARIANTS (exhaustively checked) --------------------------------
INVARIANT MasterInvariant
INVARIANT ByzantineContainment
INVARIANT LeverageCompliance
INVARIANT Agreement
INVARIANT NoDeadlock

\* -- TEMPORAL PROPERTIES (specified in the .tla; not checked in this run) -----
\* PROPERTY Liveness
\* PROPERTY ProgressUnderAttack

\* -- MODEL CONSTANTS (scaled for exhaustive tractability) --------------------
CONSTANTS
    N_AGENTS        = 4
    MAX_ROUND       = 2
    MAX_QUEUE       = 2
    BYZANTINE_COUNT = 1
    AIFMD_GROSS_LIMIT = 1
    AIFMD_NET_LIMIT   = 1
    CONC_CAP          = 1
    MAX_SCORE         = 1
